#pragma once

struct PlaygroundState
{
    Scene* scene;
    SceneBuffer scene_buffer;

    SceneObject* car;
};

static void GameInit(PlatformState* ps, AssetState* as, UIState* ui, GameState* gs)
{
    
}

static void GameUpdate(PlatformState* ps,AssetState* as,UIState* ui,GameState* gs)
{
    
}
